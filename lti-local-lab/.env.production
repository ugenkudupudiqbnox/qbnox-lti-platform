# Production environment variables for LTI Platform
# Copy this to .env (at the project root) and update for your server

### --- 🌍 Domain Configuration ---
# Protocol defaults to https unless domains are '.local'
PROTOCOL=https

# Set your production domains. URLs are automatically derived by scripts.
PRESSBOOKS_DOMAIN=pb.yourdomain.com
MOODLE_DOMAIN=moodle.yourdomain.com

### --- ⚙️ Platform Settings ---
# Important: Ensure this matches your production Moodle version.
# Affects how LTI 1.3 keys are registered (4.1, 4.4, or 5.1 supported)
MOODLE_VERSION=4.4

### --- 🔐 Internal Networking (Optional) ---
# For internal container-to-container communication (bypasses public firewalls)
# PRESSBOOKS_INTERNAL_DOMAIN=10.0.0.5
# MOODLE_INTERNAL_DOMAIN=172.18.0.2
